export interface Options {
  origin: string;
  target: string;
  silent?: boolean;
  verbose?: boolean;
}

export interface Arguments {
  help?: boolean;
  origin?: string;
  target?: string;
  silent?: boolean;
  verbose?: boolean;
}
