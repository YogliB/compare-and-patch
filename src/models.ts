export interface Options {
  origin: string;
  target: string;
  keep?: boolean;
  silent?: boolean;
  verbose?: boolean;
  watch?: boolean;
}

export interface Arguments {
  help?: boolean;
  origin?: string;
  target?: string;
  keep?: boolean;
  silent?: boolean;
  verbose?: boolean;
  watch?: boolean;
}
